# Django Configuration
SECRET_KEY=your-secret-key-here
DEBUG=1
TARGET_ENV=Dev
SECURE_SSL_REDIRECT=0

# Database Configuration (Production)
DBNAME=your-database-name
DBHOST=your-database-host
DBUSER=your-database-user
DBPASS=your-database-password

# Google OAuth Configuration
# Para obter as credenciais:
# 1. Acesse https://console.cloud.google.com/
# 2. Crie um novo projeto ou selecione um existente
# 3. Vá em "APIs e Serviços" > "Credenciais"
# 4. Clique em "Criar credenciais" > "ID do cliente OAuth 2.0"
# 5. Configure as URLs de redirecionamento autorizadas:
#    - http://localhost:8000/accounts/google/login/callback/ (para desenvolvimento)
#    - https://seu-dominio.com/accounts/google/login/callback/ (para produção)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth Configuration
# Para obter as credenciais:
# 1. Acesse https://developers.facebook.com/
# 2. Crie um novo aplicativo ou selecione um existente
# 3. Vá em "Configurações" > "Básico"
# 4. Configure "URIs de redirecionamento do OAuth válidos":
#    - http://localhost:8000/accounts/facebook/login/callback/ (para desenvolvimento)
#    - https://seu-dominio.com/accounts/facebook/login/callback/ (para produção)
FACEBOOK_CLIENT_ID=your-facebook-app-id
FACEBOOK_CLIENT_SECRET=your-facebook-app-secret

# Apple OAuth Configuration
# Para obter as credenciais:
# 1. Acesse https://developer.apple.com/account/
# 2. Vá em "Certificates, Identifiers & Profiles"
# 3. Crie um novo Service ID
# 4. Configure as URLs de retorno:
#    - http://localhost:8000/accounts/apple/login/callback/ (para desenvolvimento)
#    - https://seu-dominio.com/accounts/apple/login/callback/ (para produção)
# 5. Crie uma chave privada para Sign in with Apple
APPLE_CLIENT_ID=your-apple-service-id
APPLE_CLIENT_SECRET=your-apple-client-secret
APPLE_KEY_ID=your-apple-key-id
APPLE_PRIVATE_KEY=your-apple-private-key-content
